name: riego_inteligente_web
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0-204.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.9
  hooks_riverpod: ^1.0.0-dev.5
  url_strategy: ^0.2.0
  google_fonts: ^4.0.3
  youtube_player_flutter: ^8.1.2
  video_player:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
  - assets/images/
  - assets/images/screenshots/
  
